# Required
DATABASE_URL=postgres://kronkeeper:kronkeeper@localhost:5432/kronkeeper

# Server
API_LISTEN_ADDR=0.0.0.0:2401

# Worker pool
WORKER_COUNT=10
WORKER_QUEUE_SIZE=1000

# Scheduler
HEAP_LOOKAHEAD_LIMIT=10000
LEASE_DURATION_SECS=30
LEASE_REAPER_INTERVAL_SECS=10

# Retries
MAX_RETRY_BACKOFF_SECS=3600

# Webhooks
WEBHOOK_TIMEOUT_SECS=5
WEBHOOK_MAX_RETRIES=3

# Script execution
SCRIPT_SAFE_DIR=/opt/daemon/scripts

# Logging
RUST_LOG=info